# Foundry RPC endpoints — referenced by contracts/foundry.toml [rpc_endpoints]
# (${TESTNET_RPC_URL} / ${MAINNET_RPC_URL}).
# Required by `pnpm contracts:deploy:testnet` / `pnpm contracts:deploy:mainnet`.
# Get from Alchemy / Infura / your node provider.
TESTNET_RPC_URL=
MAINNET_RPC_URL=
